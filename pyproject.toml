[project]
name = "json-2-parquet"
version = "0.1.0"
description = "Check Parquet creation from JSON"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.39.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
